%main.container.general-main-wrap
  .text-center
    %h1.mb-30 Forgot password?
    %p.general-password-text
  = simple_form_for(resource, as: resource_name,
    url: password_path(resource_name),
    html: { method: :post, class: "general-form" }) do |f|
    .form-group.mb-25
      = f.input_field :email, id: "email", class: "form-control",
        autofocus: true
      %span.help-block
    = f.submit 'Email instructions',
      class: "btn btn-default center-block mb-20"
    .text-center
      = link_to 'Cancel',
        request.referer.present? ? request.referer : root_path,
        class: "in-gold-500 mb-20"